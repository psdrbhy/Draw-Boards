"use client"
import { Info } from "./info"

export const Canvas = () => {
    return (
        <main
            className="h-full w-full relative bg-neutral-100 touch-none"
        >
            <Info/>
        </main>
    )
}